import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        backgroundColor: '#000',
        padding: 10,
        paddingStart: 40
    },
    texto:{
        fontSize: 42,
        fontStyle: 'italic',
        fontWeight: 'bold',
        textAlign: 'center',
        color: 'red'
    }
});

export default styles;